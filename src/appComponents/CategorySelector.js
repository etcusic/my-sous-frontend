import React from 'react';

const CategorySelector = ({ supply, keyId }) => {
    return (
        <div>
           <select>
               
           </select>
        </div>
    );
}
  
export default CategorySelector